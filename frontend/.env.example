# cp .env.example .env

# URL для обращения к API backend
# Для локальной разработки:
VITE_API_URL=http://127.0.0.1:8000/api

# В Docker (docker-compose.prod.yaml) значение /api передаётся как build arg при сборке образа
# и встраивается в бандл — отдельный .env файл для Docker не нужен
